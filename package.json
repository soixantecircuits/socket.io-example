{
  "name": "socket.io-mrt-client",
  "version": "1.0.0",
  "description": "A basic socket io client that connects to meteor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^1.1.0"
  }
}
